server {
    listen 80;
    server_name {{ inventory_hostname }};

    location / {
        proxy_set_header Host $host;
        proxy_pass http://unix:/tmp/web-audio.sock;
    }

    location /static {
        alias /home/{{ ansible_ssh_user }}/sites/{{ inventory_hostname }}/source/static;
    }
}
